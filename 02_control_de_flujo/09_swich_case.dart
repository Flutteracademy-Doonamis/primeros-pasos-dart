//Escribir los dias de la semana tanto con un ciclo if - else y con swich - case, la condicion debe ser un valor random

import 'dart:math';

void main() {
  int rnd = Random().nextInt(7);

  print(rnd);
}
